################################################################################
# Environment example for NFT-Event-Ticketing
# Copy this file to `.env` and fill the values before running deployments or tests.
# Keep your real private key and API keys secret. Do NOT commit `.env` to source control.
################################################################################

# Private key for the deployer account (without 0x prefix or with it; both work).
# For local testing you can use a test account generated by Hardhat or a burner key.
PRIVATE_KEY=

# RPC URLs for networks you plan to use. Example values:
# - A local Hardhat node: http://127.0.0.1:8545
# - An Alchemy/Infura URL for Goerli, Sepolia, or other testnets
# Set only the ones you need; leave others blank.
RPC_URL_LOCAL=http://127.0.0.1:8545
RPC_URL_SEPOLIA=
RPC_URL_GOERLI=

# Optional: Etherscan API key (used by many Hardhat plugins for contract verification)
ETHERSCAN_API_KEY=

# Optional: Chain ID override (useful for some providers). Default values are handled
# by Hardhat config, so you typically don't need to set this.
CHAIN_ID=

# Example usage (PowerShell):
# $env:PRIVATE_KEY="your_key_here"; npx hardhat run --network sepolia scripts/deploy.js

################################################################################
